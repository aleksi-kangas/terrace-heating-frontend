import React from 'react';
import { Grid } from '@material-ui/core';

const Control = ({ user }) => {
  return (
    <Grid container>
      <Grid item>
        Control...
      </Grid>
    </Grid>
  )
};

export default Control;